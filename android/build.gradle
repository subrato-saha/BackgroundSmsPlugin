group 'com.j.background_sms'
version '1.0'

buildscript {
    repositories {
        google()
        jcenter()
    }

    dependencies {
        classpath 'com.android.tools.build:gradle:3.5.0'
    }
}

rootProject.allprojects {
    repositories {
        google()
        jcenter()
    }
}

apply plugin: 'com.android.library'

android {
    compileSdkVersion 33

    namespace 'dev.tranxit.background_sms'  // <--- Add this line

    defaultConfig {
        minSdkVersion 21
        targetSdkVersion 33
    }
    lintOptions {
        disable 'InvalidPackage'
    }
}
